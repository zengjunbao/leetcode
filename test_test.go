package leetcode

import (
	"fmt"
	"strings"
	"testing"
	"time"
)

func TestName(t *testing.T) {
	fmt.Println(time.Now().Unix())
	res := []string{"ac"}
	da := strings.Join(res,"")

	fmt.Println(da)

}
