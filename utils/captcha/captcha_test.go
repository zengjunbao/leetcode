package captcha

import (
	"testing"
)

func Test_Captcha(t *testing.T) {
	// captcha := NewCaptcha(100, 100, 2, "111")
	//
	// id, bs, err := captcha.GenerateCaptcha()
	// assert.NoError(t, err)
	//
	// fmt.Println(id)
	// fmt.Println(bs)
	//
	// fmt.Println(captcha.VerifyCaptcha(id, bs))
}

